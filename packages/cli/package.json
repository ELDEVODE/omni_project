{
  "name": "omni",
  "version": "0.1.0",
  "description": "omni — the OmniMesh CLI. One command to host, join, and use a sovereign AI fabric.",
  "type": "module",
  "bin": {
    "omni": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun run ./build/build.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@omnimesh/installer": "workspace:*",
    "@omnimesh/protocol": "workspace:*",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  },
  "license": "MIT"
}
